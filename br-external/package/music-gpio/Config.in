config BR2_PACKAGE_MUSIC_GPIO
    bool "music-gpio (button input driver + test)"
    help
      Installs a simple GPIO button character driver (/dev/music_input)
      and a small user-space test program to read button events.
